name: google_places
description: A new Flutter package for handle google place api that place search and details and photos and autocomplete and query autocomplete requests are available
version: 0.4.8+1
homepage: https://github.com/bazrafkan/google_places

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
